GITHUB_ISSUES_TOKEN=REPLACE_BY_YOUR_GITHUB_TOKEN
GEMINI_API_KEY=REPLACE_BY_YOUR_GEMINI_API_KEY
# Needed when DATABASE_URL uses a connection pooler (Neon, Supabase, Vercel Postgres).
# Set to the direct (non-pooler) connection string so prisma migrate deploy works.
# DIRECT_URL=
